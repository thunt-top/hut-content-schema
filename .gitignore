# Regenerated by `cargo run -p content_parser --example build_test_vectors`.
# AES-GCM's nonce is random per run, so a fresh run always diffs even with no
# manifest change; the committed copy is force-added (`git add -f`) so it's
# still tracked, just excluded from broad `git add -A`/`git add .` sweeps.
content_parser/tests/fixtures/test_vectors.json

/target
